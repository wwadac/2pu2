# 🤖 Инструкция по установке Telegram AI бота

**Создатель:** [@fuck_zaza](https://t.me/fuck_zaza)

---

## 📋 Требования

- Python 3.8 или выше
- FFmpeg (для обработки голосовых сообщений)
- IDE: Visual Studio Code или PyCharm
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))
- GROQ API Key (получить на [console.groq.com](https://console.groq.com))

---

## 🖥️ Установка на Windows

### 1. Установка Python
1. Скачайте Python с [python.org](https://www.python.org/downloads/)
2. При установке **обязательно** отметьте "Add Python to PATH"
3. Проверьте установку: откройте CMD и введите:
   ```cmd
   python --version
   ```

### 2. Установка FFmpeg
1. Скачайте FFmpeg с [ffmpeg.org](https://ffmpeg.org/download.html)
2. Распакуйте архив в `C:\ffmpeg`
3. Добавьте в PATH:
   - Откройте "Параметры системы" → "Дополнительные параметры системы"
   - "Переменные среды" → выберите Path → "Изменить"
   - Добавьте `C:\ffmpeg\bin`
4. Проверьте: откройте новый CMD и введите:
   ```cmd
   ffmpeg -version
   ```

### 3. Настройка проекта

#### В Visual Studio Code:
1. Откройте VSCode
2. File → Open Folder → выберите папку проекта
3. Откройте терминал (Ctrl + `)
4. Установите зависимости:
   ```cmd
   pip install -r requirements.txt
   ```

#### В PyCharm:
1. Откройте PyCharm
2. File → Open → выберите папку проекта
3. Откройте терминал (Alt + F12)
4. Установите зависимости:
   ```cmd
   pip install -r requirements.txt
   ```

### 4. Настройка переменных окружения
1. Создайте файл `.env` в корне проекта
2. Добавьте в него:
   ```env
   BOT_TOKEN=ваш_токен_бота
   GROQ_API_KEY=ваш_groq_api_ключ
   ADMIN_ID=ваш_telegram_id
   ```
3. Узнать свой Telegram ID можно у [@userinfobot](https://t.me/userinfobot)

### 5. Запуск бота
```cmd
python bot.py
```

---

## 🍎 Установка на macOS

### 1. Установка Homebrew (если не установлен)
Откройте Terminal и выполните:
```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

### 2. Установка Python и FFmpeg
```bash
brew install python@3.11
brew install ffmpeg
```

Проверьте установку:
```bash
python3 --version
ffmpeg -version
```

### 3. Настройка проекта

#### В Visual Studio Code:
1. Откройте VSCode
2. File → Open → выберите папку проекта
3. Откройте терминал (Ctrl + `)
4. Создайте виртуальное окружение:
   ```bash
   python3 -m venv venv
   source venv/bin/activate
   ```
5. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

#### В PyCharm:
1. Откройте PyCharm
2. File → Open → выберите папку проекта
3. PyCharm автоматически предложит создать venv
4. Откройте терминал (Ctrl + `)
5. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

### 4. Настройка переменных окружения
1. Создайте файл `.env` в корне проекта:
   ```bash
   touch .env
   nano .env
   ```
2. Добавьте:
   ```env
   BOT_TOKEN=ваш_токен_бота
   GROQ_API_KEY=ваш_groq_api_ключ
   ADMIN_ID=ваш_telegram_id
   ```
3. Сохраните (Ctrl + O, Enter, Ctrl + X)

### 5. Запуск бота
```bash
python3 bot.py
```

---

## 🐧 Установка на Linux (Ubuntu/Debian)

### 1. Обновление системы
```bash
sudo apt update
sudo apt upgrade -y
```

### 2. Установка Python и FFmpeg
```bash
sudo apt install python3 python3-pip python3-venv ffmpeg -y
```

Проверьте установку:
```bash
python3 --version
ffmpeg -version
```

### 3. Настройка проекта

#### В Visual Studio Code:
1. Установите VSCode:
   ```bash
   sudo snap install code --classic
   ```
2. Откройте проект:
   ```bash
   code .
   ```
3. Откройте терминал (Ctrl + `)
4. Создайте виртуальное окружение:
   ```bash
   python3 -m venv venv
   source venv/bin/activate
   ```
5. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

#### В PyCharm:
1. Скачайте PyCharm с [jetbrains.com](https://www.jetbrains.com/pycharm/download/#section=linux)
2. Откройте проект
3. Создайте venv через настройки PyCharm
4. Установите зависимости в терминале:
   ```bash
   pip install -r requirements.txt
   ```

### 4. Настройка переменных окружения
```bash
nano .env
```

Добавьте:
```env
BOT_TOKEN=ваш_токен_бота
GROQ_API_KEY=ваш_groq_api_ключ
ADMIN_ID=ваш_telegram_id
```

Сохраните (Ctrl + O, Enter, Ctrl + X)

### 5. Запуск бота
```bash
python3 bot.py
```

### 6. Запуск в фоновом режиме (опционально)
```bash
nohup python3 bot.py > bot.log 2>&1 &
```

Просмотр логов:
```bash
tail -f bot.log
```

Остановка:
```bash
pkill -f bot.py
```

---

## 🖥️ Установка на сервер

Для установки бота на VPS/сервер с автозапуском и мониторингом используйте подробную инструкцию:

👉 **[Инструкция по установке на сервер](https://t.me/StriverDev/191)**

Там описаны:
- Установка через systemd
- Автозапуск при перезагрузке
- Мониторинг логов
- Обновление бота
- Настройка безопасности

---

## ⚙️ Базовая настройка бота

### Получение токенов

1. **Telegram Bot Token:**
   - Напишите [@BotFather](https://t.me/BotFather)
   - Отправьте `/newbot`
   - Следуйте инструкциям
   - Скопируйте полученный токен

2. **GROQ API Key:**
   - Зайдите на [console.groq.com](https://console.groq.com)
   - Зарегистрируйтесь
   - Создайте новый API ключ
   - Скопируйте ключ

3. **Telegram ID:**
   - Напишите [@userinfobot](https://t.me/userinfobot)
   - Скопируйте ваш ID

### Настройка Business аккаунта

1. Убедитесь, что у вас есть Telegram Business подписка
2. Запустите бота командой `/start`
3. В настройках Telegram Business подключите бота к вашему аккаунту

---

## 🎯 Основные команды бота

- `/start` - Меню настройки AI
- `/clear` - Очистить память диалога

### Настройка личности AI

Бот поддерживает два формата конфигурации:

1. **Простой текстовый промпт:**
   ```
   Ты - дружелюбный помощник. Отвечай кратко и по делу.
   ```

2. **JSON конфигурация с детальными настройками:**
   ```json
   {
     "name": "ассистент",
     "personality": {
       "communication_style": {
         "writing_style": {
           "case": "lowercase_only",
           "max_words_per_message": 15
         }
       }
     }
   }
   ```

---

## 🔧 Решение проблем

### Windows

**Ошибка: 'python' is not recognized**
```cmd
# Используйте py вместо python
py bot.py
```

**Ошибка: ffmpeg not found**
```cmd
# Переустановите FFmpeg и проверьте PATH
where ffmpeg
```

### macOS/Linux

**Ошибка: Permission denied**
```bash
chmod +x bot.py
```

**Ошибка: Module not found**
```bash
# Убедитесь, что venv активирован
source venv/bin/activate
pip install -r requirements.txt
```

### Общие проблемы

**Бот не отвечает в Business чатах:**
- Проверьте, что Business подписка активна
- Убедитесь, что бот подключен в настройках Business

**Голосовые не распознаются:**
- Проверьте установку FFmpeg: `ffmpeg -version`
- Убедитесь, что SpeechRecognition установлен

**Rate limit ошибки:**
- Бот автоматически обрабатывает rate limits
- Подождите 5-10 секунд между запросами

---

## 📝 Структура проекта

```
project/
├── bot.py                      # Основной файл бота
├── requirements.txt            # Зависимости
├── .env                        # Переменные окружения (создать вручную)
├── bot_data.db                 # База данных (создается автоматически)
└── business_connections.json   # Business подключения (создается автоматически)
```

---

## 💡 Дополнительная информация

- **Создатель:** [@fuck_zaza](https://t.me/fuck_zaza)
- **Инструкция для сервера:** [t.me/StriverDev/191](https://t.me/StriverDev/191)
- **Поддержка:** Обращайтесь к создателю в Telegram

---

## ⚠️ Важные замечания

1. Никогда не публикуйте файл `.env` в публичных репозиториях
2. Храните токены в безопасности
3. Регулярно обновляйте зависимости: `pip install --upgrade -r requirements.txt`
4. Для production использования рекомендуется серверная установка
5. Бот использует SQLite - для высоких нагрузок рассмотрите PostgreSQL

---

**Успешной работы с ботом! 🚀**